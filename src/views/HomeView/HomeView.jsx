import React from 'react';
import styles from './home.module.scss';

const HomeView = () => (
  <div className={styles.home__container}>
    <h1 className={styles.home__title}>HELLO USER</h1>
  </div>
);

export default HomeView;
